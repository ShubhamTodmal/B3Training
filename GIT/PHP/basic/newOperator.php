<?php

    $a = "shubham";
    $b = " todmal";
    echo $a.$b;//here we got concatination
    $a.=$b;
    echo $a;// here second string append on 1st string

    $x=10;$y=20;
    if($x==10 xor $b==25)
    {
        echo 'done';//here we see if x and y is both true and both are flase then it not work it return false
        //if one on of them is true then it return true
    }

    if($x<>$y)
    {
        echo 'not equal';//we also use this for not equal
        //or we can use != this one
    }

?>